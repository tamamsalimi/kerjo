export type Filters = {
  categories: string[];
  job_type: string;
  max_distance: number;
  max_pay: number;
  max_experience: number;
};

export type Coordinates = {
  latitude: number;
  longitude: number;
};
