# Testing-only. Copy to dev/multi-device.env and fill in local values.
# Never commit the copied file. Do not use these values in production.

# Reserved ngrok hostname, without https://
NGROK_DOMAIN=

# Optional. Needed only if `ngrok config check` is not already authenticated.
NGROK_AUTHTOKEN=
